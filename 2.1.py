#!/usr/bin/python3
# -*- coding:utf-8 -*-
from sys import getsizeof
import os

colors = ['black', 'white']
sizes = ['S', 'M', 'L']
tshirts = [(color, size) for color in colors for size in sizes]

a = (1, 2)

b = [1, 2]
t = (1, 2, [30, 40])
